# Only buildings.json is too large to commit (61MB); it is served from object
# storage via window.ATLAS_BUILDINGS_URL. The OSM context layers are small and tracked.
data/buildings.json
data/*_fetch_progress.json

# Python / env
__pycache__/
*.pyc
venv/
.venv/

# Local tooling / OS cruft
.claude/
.DS_Store
Thumbs.db

# deploy cruft
data/buildings_fetch_progress.json
